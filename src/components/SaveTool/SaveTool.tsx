import React from 'react';

interface SaveToolProps {

}

const SaveTool: React.FC<SaveToolProps> = (props) => {
    return (
        <div className="tools__save " {...props}>

        </div>
    );
};

export default SaveTool;
