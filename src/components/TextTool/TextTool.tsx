import React from 'react';

interface TextToolProps {

}

const TextTool: React.FC<TextToolProps> = (props) => {
    return (
        <div className="tools__text tools__item" {...props}>

        </div>
    );
};

export default TextTool;
